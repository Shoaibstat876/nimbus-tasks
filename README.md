# Nimbus Tasks

Monorepo for Nimbus Tasks.

## Apps
- `apps/web` — Next.js frontend

## Packages
- `packages/ui` — shared UI primitives + theme tokens
- `packages/config` — shared eslint/tsconfig presets

## Commands
- `pnpm dev`
- `pnpm build`
- `pnpm ci`

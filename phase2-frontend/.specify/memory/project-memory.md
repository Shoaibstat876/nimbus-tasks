# Project Memory — Nimbus Phase 2 Frontend

## Goal
Deliver a Phase 2 full-stack tasks UI that supports:
- login → token → authenticated task CRUD
- owner-only task access
- production-grade UI states and validation
- evidence for review

## Tech
- Next.js (App Router) + TypeScript
- FastAPI backend (separate service)
- Bearer token auth
- Neon + SQLModel backend data

## Proof Strategy
- Specs in `.specify/specs/`
- Evidence saved in `evidence/`
- Short demo video shows: spec → implementation → evidence

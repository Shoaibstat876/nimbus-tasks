cd "D:\Shoaib Project\nimbus-tasks\phase2-backend\api"

Invoke-RestMethod `
  -Method POST `
  -Uri "http://127.0.0.1:8000/api/auth/login" `
  -Headers @{ "Content-Type" = "application/x-www-form-urlencoded" } `
  -Body "username=new_tester@example.com&password=WRONGPASS"

  # terminal result :
  ✅ VERDICT: ACCEPTED (Wrong-Credentials Auth Gate)

Your command:

Endpoint: POST /api/auth/login

Username: valid

Password: WRONGPASS

Response body: {"detail":"Invalid credentials"}

PowerShell threw a WebException (expected for 4xx)

This proves:

Credentials are validated

Invalid credentials are rejected

Proper error message is returned

Auth is not permissive

🔐 Wrong-credentials rejection = PASSED

--------------------------------------------------------------------------------
try {
  Invoke-RestMethod `
    -Method POST `
    -Uri "http://127.0.0.1:8000/api/auth/login" `
    -Headers @{ "Content-Type" = "application/x-www-form-urlencoded" } `
    -Body "username=new_tester@example.com&password=WRONGPASS"
  "❌ Unexpected: login succeeded"
} catch {
  $_.Exception.Response.StatusCode.value__
}

# terminal result:
✅ PERFECT. ACCEPTED.
This is examiner-grade proof.

What you proved (no debate)

Wrong password returns 401 ✅

Error message is Invalid credentials ✅

Auth is correctly enforcing credential validation ✅

So the specific claim “auth fails with wrong info” is now fully proven.

✅ AUTH GATE — FINAL PASS (Locked)

I am now closing Auth Gate. We will not touch it again unless you break something